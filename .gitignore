# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (do NOT commit these)
.env*
!.env.example
.env

# Google Cloud credentials (NEVER commit these!)
*credentials*.json
*-credentials.json
pravartak-*.json
flash-precept-*.json
gcp-credentials.json

# Backend environment and credentials
# deprecated: backend/.env removed in favor of single root .env
backend/.env
backend/gcp-credentials.json
backend/audio_files/
audio_files
backend/__pycache__/

# Python virtual environments
backend/venv/
backend/.venv/
venv/
.venv/
*.pyc

# Firebase Admin SDK service account keys (NEVER commit these!)
adminkey.json
*-service-account-key.json
firebase-adminsdk-*.json

# Google Cloud Platform credentials (NEVER commit these!)
backend/gcp-credentials.json
gcp-credentials.json
*-credentials.json

# typescript
*.tsbuildinfo
next-env.d.ts

# clerk (legacy authentication)
.clerk/

# Firebase hosting files (using App Engine instead)
firebase.json
.firebaserc

# Firebase Data Connect (unused)
dataconnect/
src/dataconnect-generated/

# Empty GitHub workflows
.github/workflows/

# Backup files
backups/
*.backup
*.backup_*

# Local secrets directory (keep local, do not commit)
.secrets/
secrets/

# Documentation (temporary guides)
docs/
DATA_FLOW_GUIDE.md
MISSING_FEATURES_GUIDE.md
FIREBASE_ENV_SETUP.md
DEPLOYMENT.md
FIREBASE_CONFIG_FIX.md

# Setup and documentation files (keep these)
!INTERVIEW_SIMULATOR_SETUP.md
!INTERVIEW_SIMULATOR_API_CONFIG.md
!INTEGRATION_COMPLETE.md
!setup-backend.ps1

# Migration scripts (no longer needed)
scripts/cleanup-and-setup.ps1
scripts/cloud-sql-setup.ps1
scripts/fresh-gcp-setup.ps1
scripts/migrate-data.js
scripts/pre-migration-check.js
scripts/verify-migration.js

# Build artifacts
out/

# Cloud SQL Proxy
cloud-sql-proxy.exe
cloud-sql-proxy

# Debug logs
*.log
firebase-debug.log

# Old config files
.eslintrc.json

# AWS Amplify Gen 2
amplify_outputs.json
amplify/data/resource.ts.bak
.amplify/
node_modules/.amplify/

# Scripts folder
scripts/

test-cognito-config.js